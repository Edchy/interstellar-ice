<div class="big-words">
  <div class="inner">
    <h3 data-aos="fade-up-right">Nope</h3>
    <h3 data-aos="fade-up-left">Nope</h3>
    <h3 data-aos="fade-up-right"></h3>
    <h3 data-aos="fade-up-left">Nope</h3>
    <h3 data-aos="fade-up-right">Nope</h3>
    <h3 data-aos="fade-up-left">Nope</h3>
  </div>
</div>
<script>
  import AOS from "aos";
  import "aos/dist/aos.css";
  document.addEventListener("astro:page-load", () => {
    AOS.init({
      duration: 500,
      easing: "ease-in-out",
      once: false,
      mirror: false,
    });
  });
</script>
<style>
  .big-words {
    --hdr-gradient: radial-gradient(
      farthest-corner circle at 50% 50% in oklab,
      var(--clr-accent) 12% 12%,
      var(--yang) 56% 56%
    );

    /* width: 100%;  */
    /* -webkit-mask: var(--mask-edge-zig-zag-top); */
    background: var(--hdr-gradient);
    z-index: calc(infinity - 1);
    left: 0;
    overflow-x: clip;

    @media (prefers-reduced-motion: no-preference) {
      animation: gradient-change 6s linear infinite alternate;
    }

    .inner {
      /* background-color: red; */
      /* background-color: aliceblue; */
      margin-inline: auto;
      width: 100%;
      padding-block: 5rem;
      text-align: center;
      /* display: grid; */
      /* grid-template-columns: repeat(auto-fit, minmax(500px, 1fr)); */

      @media (width > 1200px) {
        width: min(100%, 901px);

        /* tinker with these values */
        width: clamp(901px, 70vw, 1100px);
        width: clamp(801px, 70vw, 1100px);
      }
    }

    h3 {
      /* text-box: trim-both cap alphabetic; */
      font-size: clamp(5rem, 1rem + 20vw, 20rem);
      font-family: "Kass";
      font-family: var(--font-heading);
    }
  }

  @keyframes gradient-change {
    to {
      --clr-accent: oklch(30% 0.29 149);
      /* --clr-accent: oklch(from var(--clr-accent) l c 249); */
      /* --white: oklch(90% 0 91); */
    }
  }
</style>
